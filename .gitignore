# ===================== 本地运行资源 =====================
.minecraft
jdk-21.0.11
game
game.zip
data
# JRE 下载的压缩包（可从 Azul CDN 重新下载，无需入库）
*.tar.gz
*.zip

# ===================== 依赖 =====================
node_modules
.pnp
.pnp.js

# ===================== 构建产物 =====================
dist
dist-ssr
build
.output
.output/*
!.output/.gitkeep

# ===================== 环境变量 =====================
.env
.env.local
.env.*.local

# ===================== Rust / Tauri 编译缓存与生成文件 =====================
target/
client/src-tauri/.tauri/
client/src-tauri/gen/

# Tauri 签名密钥（重要！不要提交密钥）
*.pem
*.key

# ===================== Golang server 子项目 =====================
# Go 服务器运行时数据目录（存放 game.zip 等下载文件）
server/golang/data/
server/golang/bin/
server/golang/*.exe
server/golang/*.exe~
server/golang/*.dll
server/golang/*.so
server/golang/*.dylib
server/golang/*.test
server/golang/*.out
server/golang/coverage.out

# ===================== 操作系统文件 =====================
.DS_Store
.AppleDouble
.LSOverride
._*
__MACOSX
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*~
.fuse_hidden*
.directory
.Trash-*

# ===================== 编辑器 / 日志 / 临时文件 =====================
.idea/
.vscode/
*.swp
*.swo
*.log
tmp/
temp/
download/
*.tmp
*.bak
